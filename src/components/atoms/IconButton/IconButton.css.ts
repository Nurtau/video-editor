import { style } from "@vanilla-extract/css";

// @NOW: add correct styles
// @NOW: probably make recipe with different variants
export const iconButtonStyles = style({
  backgroundColor: "transparent",
  border: "none",
  cursor: "pointer",
});
